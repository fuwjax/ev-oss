language: java
jdk:
  - oraclejdk8
before_install:
  - mvn license:format
  - mvn versions:use-latest-releases
  - git push -m “updating license and dependencies”
before_script:
  - mvn versions:set -DnewVersion=1.$TRAVIS_BUILD_NUMBER
after_script:
  - mvn scm:tag